# Exercise 1: Getting Started With ImageLab

In this google drive, the necessary files for this week's exercise are included such as:

## (0) modified/added code

This includes:

1. gray1.cpp (converting color image to gray + gray strip)
2. box1.cpp (creating a rectangular box in the image)
3. mergegray1.cpp (creates transition of gray by slowly covering the right half with gray values)
4. mergegray2.cpp (creates a transition of gray values by percentage with the highest converting the right half with full gray)
5. circletriangles.cpp (creates a triangle for both eyes and a circle for the head)
6. tools.c (made a function inside for merging two input images)

#### Notes

- There are two files that were made for the merging of two images and slowly transitioning them with gray values. I misunderstood the direction so I ended up making two different functions. Added them both just for the documentation and it also served as a little experiment.

## (1) input images

This includes the selfies that I used for the programs.

## (2) output images

This contains the output images made by the programs in the (0) folder.

## (3) lab reflection journal output

This contains the pdf file for my answers for the journal questions.
